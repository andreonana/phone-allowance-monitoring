"""
Racine du projet : présent uniquement pour que pytest insère ce dossier dans
`sys.path`, rendant le package `app` importable depuis `tests/` sans
installation (`pip install -e .`) ni fichier `__init__.py` dans `tests/`.
"""
